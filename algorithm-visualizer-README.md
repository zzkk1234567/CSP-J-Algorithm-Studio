# 📚 CSP-J 算法可视化系统 - 增强版使用说明

## 🎯 概述

这是一个专为CSP-J考试准备的算法可视化学习系统，通过动画演示、交互控制和详细讲解，帮助学生深入理解各种算法的执行过程。

## ✨ 主要特性

### 1. 完整的算法覆盖
- **排序算法**：冒泡、选择、插入、快速、归并排序
- **搜索算法**：线性查找、二分查找
- **图算法**：DFS、BFS、Dijkstra最短路径

### 2. 交互式控制
- ▶️ **播放/暂停**：控制动画播放和暂停
- ⏭️ **步进执行**：逐步执行算法，观察每一步变化
- 🔄 **重置**：重新开始算法演示
- 🎚️ **速度调节**：100ms-2000ms可调节动画速度

### 3. 数据自定义
- 支持自定义输入数据
- 一键生成随机测试数据
- 实时数据可视化展示

### 4. 教育功能
- 📖 **步骤说明**：实时显示当前执行步骤的详细说明
- 📊 **复杂度分析**：展示最佳、平均、最坏时间复杂度
- 📈 **统计信息**：实时显示比较次数、交换次数、访问次数
- ⏱️ **执行时间**：记录算法执行总时间

## 🚀 快速开始

### 方法1：直接使用演示页面

1. 在浏览器中打开 `algorithm-visualizer-demo.html`
2. 选择算法类别（排序/搜索/图算法）
3. 选择具体算法
4. 点击控制按钮开始演示

### 方法2：集成到现有项目

1. 引入核心JavaScript文件：
```html
<script src="algorithm-visualizer.js"></script>
```

2. 创建容器元素：
```html
<div id="my-visualizer"></div>
```

3. 初始化可视化器：
```javascript
// 创建排序算法可视化器
const sortViz = new SortingVisualizer('my-visualizer', 'bubble');
sortViz.setData([64, 34, 25, 12, 22, 11, 90]);
sortViz.init();
sortViz.initVisualization();

// 创建搜索算法可视化器
const searchViz = new SearchVisualizer('my-visualizer', 'binary');
searchViz.setData([1, 3, 5, 7, 9, 11, 13, 15]);
searchViz.setTarget(7);
searchViz.init();
searchViz.initVisualization();

// 创建图算法可视化器
const graphViz = new GraphVisualizer('my-visualizer', 'dfs');
graphViz.setGraph(nodes, edges);
graphViz.init();
graphViz.initVisualization();
```

## 📖 API 文档

### AlgorithmVisualizer 基类

#### 构造函数
```javascript
new AlgorithmVisualizer(containerId, options)
```
- `containerId`: 容器元素的ID
- `options`: 配置选项
  - `speed`: 动画速度（毫秒）
  - `colors`: 颜色配置对象

#### 主要方法
- `play()`: 播放动画
- `pause()`: 暂停动画
- `stepForward()`: 步进执行
- `reset()`: 重置动画
- `setSpeed(speed)`: 设置动画速度
- `setCustomData()`: 设置自定义数据
- `generateRandomData(size, min, max)`: 生成随机数据

### SortingVisualizer 排序可视化器

#### 支持的算法
- `'bubble'`: 冒泡排序
- `'selection'`: 选择排序
- `'insertion'`: 插入排序
- `'quick'`: 快速排序
- `'merge'`: 归并排序

#### 示例
```javascript
const visualizer = new SortingVisualizer('container', 'quick');
visualizer.setData([5, 2, 8, 1, 9, 3]);
visualizer.init();
visualizer.initVisualization();
visualizer.play();
```

### SearchVisualizer 搜索可视化器

#### 支持的算法
- `'linear'`: 线性查找
- `'binary'`: 二分查找

#### 示例
```javascript
const visualizer = new SearchVisualizer('container', 'binary');
visualizer.setData([1, 3, 5, 7, 9, 11]);
visualizer.setTarget(7);
visualizer.init();
visualizer.initVisualization();
visualizer.play();
```

### GraphVisualizer 图可视化器

#### 支持的算法
- `'dfs'`: 深度优先搜索
- `'bfs'`: 广度优先搜索
- `'dijkstra'`: Dijkstra最短路径

#### 示例
```javascript
const nodes = [
    { id: 'A', label: 'A', x: 100, y: 100 },
    { id: 'B', label: 'B', x: 200, y: 100 }
];
const edges = [
    { from: 'A', to: 'B', directed: false }
];

const visualizer = new GraphVisualizer('container', 'dfs');
visualizer.setGraph(nodes, edges);
visualizer.init();
visualizer.initVisualization();
visualizer.play();
```

## 🎨 自定义样式

### 颜色配置
```javascript
const options = {
    colors: {
        default: '#3b82f6',      // 默认颜色
        comparing: '#ef4444',    // 比较时的颜色
        completed: '#10b981',    // 完成后的颜色
        pivot: '#f59e0b',        // 基准元素颜色
        auxiliary: '#8b5cf6',    // 辅助元素颜色
        found: '#22c55e',        // 找到目标的颜色
        current: '#fbbf24'       // 当前元素颜色
    }
};
```

### 速度配置
```javascript
const options = {
    speed: 1000  // 动画速度（毫秒）
};
```

## 🔧 高级功能

### 监听事件
```javascript
// 动画完成事件
visualizer.onComplete = function() {
    console.log('算法执行完成！');
    console.log(`比较次数: ${this.state.comparisons}`);
    console.log(`交换次数: ${this.state.swaps}`);
};
```

### 获取统计信息
```javascript
const stats = {
    comparisons: visualizer.state.comparisons,
    swaps: visualizer.state.swaps,
    accessCount: visualizer.state.accessCount,
    elapsedTime: visualizer.state.endTime - visualizer.state.startTime
};
```

## 📚 教育建议

### 1. 课堂演示
- 使用大屏幕展示算法执行过程
- 暂停在关键步骤进行讲解
- 比较不同算法的执行效率

### 2. 学生自学
- 自定义数据观察算法行为
- 使用步进功能理解每一步
- 查看算法原理和代码实现

### 3. 练习测试
- 预测算法下一步操作
- 分析算法复杂度
- 比较不同输入的执行效果

## 🐛 常见问题

### Q: 动画速度太快/太慢？
A: 使用速度滑块调节，范围100-2000ms

### Q: 如何输入自定义数据？
A: 在输入框中输入逗号分隔的数字，如：5,3,8,2,7

### Q: 为什么二分查找的数组自动排序了？
A: 二分查找算法要求数组必须有序，系统会自动排序输入数据

### Q: 如何查看算法的C++代码？
A: 点击"📖 算法原理"按钮查看完整的原理说明和代码实现

## 📝 更新日志

### v2.0.0 (2024-01)
- ✨ 全新的面向对象架构
- ✨ 添加交互式控制功能
- ✨ 支持自定义数据输入
- ✨ 实时统计信息展示
- ✨ 步骤说明和复杂度分析
- 🐛 修复动画同步问题
- 🎨 改进视觉效果

### v1.0.0 (2023-12)
- 🎉 初始版本发布
- 基础排序算法动画
- 简单的可视化展示

## 💡 贡献指南

欢迎贡献新的算法实现！请遵循以下步骤：

1. 继承相应的基类（AlgorithmVisualizer）
2. 实现必要的方法
3. 添加算法说明和复杂度信息
4. 提供使用示例
5. 更新文档

## 📄 许可证

本项目仅供教育用途，专为CSP-J考试准备而设计。

## 🙏 致谢

感谢所有为CSP-J教育事业做出贡献的老师和同学们！

---

**祝你在CSP-J考试中取得优异成绩！** 🎯✨
